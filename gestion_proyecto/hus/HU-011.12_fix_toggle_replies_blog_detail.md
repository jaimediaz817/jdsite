# HU-011.12 â€” Fix: toggle de respuestas (`.jd-toggle-replies-btn`) en blog detail

> **Origen:** Al hacer clic en el botÃ³n "Ver N respuesta(s)" de un comentario en la pÃ¡gina de detalle de un artÃ­culo, las respuestas anidadas **no se expanden/colapsan**. La consola del navegador sÃ­ muestra el `console.log('Toggle button clicked', btn)` del handler JS, lo que confirma que el listener se dispara, pero visualmente no ocurre nada.

> **Objetivo:** Restaurar el comportamiento esperado del toggle de respuestas (mostrar/ocultar el bloque `.jd-replies` de un comentario) de forma fiable, **sin introducir nuevas dependencias** y sin romper ningÃºn comportamiento existente.

---

## ðŸŸ¡ REGLAS DE ORO

1. **Aditivo:** No se modifica el HTML del template ni se elimina cÃ³digo existente; sÃ³lo se reemplaza la funciÃ³n `initToggleReplies` y se aÃ±ade 1 regla CSS.
2. **Sin dependencias nuevas:** Se usa `classList.toggle()` y `addEventListener` (nativo del navegador).
3. **Idempotencia:** El listener de click se registra una sola vez en `document`, no en cada llamada a `initToggleReplies()`. Esto evita la duplicaciÃ³n que actualmente ocurre al hacer scroll infinito.
4. **Cero ruptura:** El botÃ³n sigue funcionando con la misma firma visual (chevron + label "Ver/Ocultar"). El scroll infinito, el botÃ³n "Responder", el submit de comentarios y la eliminaciÃ³n como superuser **no se tocan**.
5. **Mantener el layout flex:** El contenedor `.jd-replies` tiene `display: flex` en CSS; cualquier fix debe respetar ese layout (no usar `display: block` inline).
6. **Alineado con el diagnÃ³stico:** Esta HU es la consecuencia directa del documento `gestion_proyecto/diagnosticos/diagnostico_toggle_replies_blog_detail.md`.

---

## ðŸ“‹ CONTEXTO ACTUAL

### Archivos involucrados

| Archivo                                           | Rol                                                                           | LÃ­neas relevantes                        |
| ------------------------------------------------- | ----------------------------------------------------------------------------- | ---------------------------------------- |
| `backend/blog/templates/blog/blog_detail.html`    | Template del detalle del blog (HTML del botÃ³n y del contenedor `.jd-replies`) | 516â€“564                                  |
| `backend/blog/static/blog/js/comments_handler.js` | Handler JS del toggle (`initToggleReplies`)                                   | 228â€“265                                  |
| `backend/blog/static/blog/css/blog_detail.css`    | Estilos `.jd-replies`, `.jd-toggle-replies-btn`                               | 559â€“588, 2172â€“2180, 993â€“1010 (huÃ©rfanos) |

### HTML del botÃ³n y contenedor (lÃ­neas 516â€“564)

```html
{% if comment.replies.all %}
  <button class="jd-toggle-replies-btn" type="button">
    <i class="fas fa-chevron-down"></i>
    <span>Ver</span>
    {{ comment.replies.count }} respuesta{{ comment.replies.count|pluralize:",s" }}
  </button>

  <!-- Respuestas anidadas -->
  <div class="jd-replies" style="display:none;">
  {% for reply in comment.replies.all %}
    <div class="jd-reply">â€¦</div>
  {% endfor %}
  </div>
{% endif %}
```

### CSS real de `.jd-replies` (lÃ­neas 2172â€“2180)

```css
.jd-replies {
    margin-top: 0.75rem;
    padding-left: 0.75rem;
    border-left: 2px solid #f0e6ff;
    display: flex;          /* âš ï¸ display por CSS = flex */
    flex-direction: column;
    gap: 0.75rem;
}
```

### JS actual (lÃ­neas 228â€“265) â€” problemÃ¡tico

```javascript
function initToggleReplies() {
    document.addEventListener('click', function(e) {     // âš ï¸ listener NUEVO cada llamada
        var btn = e.target.closest('.jd-toggle-replies-btn');
        if (!btn) return;
        console.log('Toggle button clicked', btn);        // âœ… se ve en consola
        var commentEl = btn.closest('.jd-comment');
        if (!commentEl) return;
        var repliesEl = commentEl.querySelector('.jd-replies');
        if (!repliesEl) return;
        var isOpen = repliesEl.style.display === 'block';   // âŒ nunca true al inicio
        repliesEl.style.display = isOpen ? 'none' : 'block'; // âŒ rompe el flex del CSS
        // â€¦icono y labelâ€¦
    });
}
```

Y se llama desde 2 sitios:
- `DOMContentLoaded` (al final del archivo) â†’ registra el listener.
- `loadMoreComments()` (~lÃ­nea 285, scroll infinito) â†’ **vuelve a llamar a `initToggleReplies()`**, agregando **otro listener al `document`** sin remover el previo.

### CSS huÃ©rfano (lÃ­neas 993â€“1010)

```css
/* Transiciones Alpine para el contenedor de respuestas */
.jd-replies { overflow: hidden; }
.jd-transition { transition: all 0.35s cubic-bezier(0.4, 0, 0.2, 1); }
.jd-transition-start { opacity: 0; transform: translateY(-12px) scale(0.97); }
.jd-transition-end   { opacity: 1; transform: translateY(0) scale(1); }
```

Las clases `jd-transition*` **no se usan en ningÃºn sitio** del HTML ni del JS. Son residuo de un intento previo de animar con Alpine y deben limpiarse (fase opcional).

---

## ðŸ› CAUSA RAÃZ (resumen del diagnÃ³stico completo)

1. **Conflicto `display: block` vs `display: flex`:** el JS fuerza `style.display = 'block'`, lo cual **rompe el `flex-direction: column`** del CSS.
2. **Listener duplicado por cada scroll infinito:** tras N cargas, un solo clic dispara N veces y los valores de `isOpen` alternan, anulando el efecto.
3. **No es un problema de Alpine.js perdido** â€” el handler existe y se dispara. La causa estÃ¡ en la interacciÃ³n JS â†” CSS.

ðŸ“„ **DiagnÃ³stico completo:** `gestion_proyecto/diagnosticos/diagnostico_toggle_replies_blog_detail.md`

---

## ðŸ”§ PLAN DE IMPLEMENTACIÃ“N (fases de ~5â€“10 min cada una)

### Fase 1: Reemplazar `initToggleReplies` en `comments_handler.js` âœ…

**Archivo:** `backend/blog/static/blog/js/comments_handler.js`

Reemplazar la funciÃ³n completa por la versiÃ³n idempotente y basada en clases CSS:

```javascript
function initToggleReplies() {
    // âœ… Idempotente: solo aÃ±ade el listener una vez al document
    if (document._jdToggleRepliesInit) return;
    document._jdToggleRepliesInit = true;

    document.addEventListener('click', function(e) {
        var btn = e.target.closest('.jd-toggle-replies-btn');
        if (!btn) return;

        var commentEl = btn.closest('.jd-comment');
        if (!commentEl) return;
        var repliesEl = commentEl.querySelector('.jd-replies');
        if (!repliesEl) return;

        // âœ… Usar clase CSS en vez de style.display (preserva display:flex)
        var isOpen = repliesEl.classList.contains('is-open');
        repliesEl.classList.toggle('is-open', !isOpen);

        // Icono
        var icon = btn.querySelector('i');
        if (icon) {
            if (isOpen) {
                icon.classList.remove('fa-chevron-up');
                icon.classList.add('fa-chevron-down');
            } else {
                icon.classList.remove('fa-chevron-down');
                icon.classList.add('fa-chevron-up');
            }
        }
        // Label
        var labelSpan = btn.querySelector('span');
        if (labelSpan) {
            labelSpan.textContent = isOpen ? 'Ver' : 'Ocultar';
        }
        e.preventDefault();
    });
}
```

**ValidaciÃ³n:** recargar la pÃ¡gina (sin scroll) â†’ al hacer clic en "Ver N respuesta(s)" deben mostrarse las respuestas, el icono debe pasar a `fa-chevron-up` y el label a "Ocultar".

---

### Fase 2: AÃ±adir regla CSS `.jd-replies.is-open` âœ…

**Archivo:** `backend/blog/static/blog/css/blog_detail.css`

AÃ±adir **al final del bloque "Respuestas anidadas" (despuÃ©s de la lÃ­nea 2180)**, o donde estÃ© la regla `.jd-replies`:

```css
.jd-replies.is-open {
    display: flex;   /* sobrescribe el none inline y mantiene flex */
}
```

(El estado cerrado lo da el `style="display:none"` inline que ya tiene el HTML, asÃ­ que **no hace falta tocar el template**.)

**ValidaciÃ³n:** confirmar que:
- El botÃ³n "Ver" â†’ al hacer clic muestra las respuestas **una debajo de otra con el `border-left` morado y el `padding-left`** (es decir, el layout flex se mantiene).
- El botÃ³n "Ocultar" â†’ al hacer clic las oculta de nuevo.
- El icono y el label cambian correctamente.

---

### Fase 3: Validar comportamiento tras scroll infinito âœ…

1. Abrir un artÃ­culo con **mÃ¡s de 10 comentarios** (para forzar el scroll).
2. Hacer scroll hasta que se carguen al menos 2 lotes.
3. Verificar en la consola del navegador:
   - Antes de cualquier scroll: `getEventListeners(document).click.length` muestra N listeners de click (los ya existentes).
   - Tras varios scrolls: el nÃºmero **no debe aumentar significativamente** por culpa del toggle.
4. Hacer clic en cualquier botÃ³n "Ver respuestas" de los comentarios reciÃ©n cargados.
5. **Resultado esperado:** el toggle funciona de forma consistente (sin parpadeos ni estados pegados).

**ValidaciÃ³n adicional (opcional, en consola del navegador):**

```javascript
// Verificar que el listener no se duplica
window._jdToggleTest = 0;
document.addEventListener('click', () => { window._jdToggleTest++; });
// Luego hacer 3 clics y leer window._jdToggleTest â†’ deberÃ­a sumar 3, no mÃ¡s
```

---

### Fase 4 (opcional): Limpiar CSS huÃ©rfano de Alpine âšª

**Archivo:** `backend/blog/static/blog/css/blog_detail.css` (lÃ­neas 993â€“1010)

Si confirmamos que no se va a usar Alpine.js para animar las respuestas, **comentar** (no eliminar) el bloque:

```css
/* Transiciones Alpine para el contenedor de respuestas */
/* COMENTADO 09/06/2026 - Refactor a classList.toggle (HU-011.12)
.jd-replies {
    overflow: hidden;
}
.jd-transition { transition: all 0.35s cubic-bezier(0.4, 0, 0.2, 1); }
.jd-transition-start { opacity: 0; transform: translateY(-12px) scale(0.97); }
.jd-transition-end   { opacity: 1; transform: translateY(0) scale(1); }
*/
```

**RazÃ³n de comentarlo y no eliminarlo:** nuestras reglas de oro indican no borrar cÃ³digo existente. Si en el futuro se quiere volver a animar, basta con descomentar.

**ValidaciÃ³n:** `python manage.py check` sin errores y la pÃ¡gina renderiza idÃ©ntica visualmente.

---

## ðŸ“¦ IMPACTO EN ARCHIVOS

| Archivo                                           | Cambio                                                                                                   | Tipo                 |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | -------------------- |
| `backend/blog/static/blog/js/comments_handler.js` | Reemplazar funciÃ³n `initToggleReplies` (idempotente + classList)                                         | ModificaciÃ³n aditiva |
| `backend/blog/static/blog/css/blog_detail.css`    | AÃ±adir regla `.jd-replies.is-open { display: flex; }` (Fase 2) y comentar CSS huÃ©rfano (Fase 4 opcional) | Aditivo              |
| `backend/blog/templates/blog/blog_detail.html`    | **SIN CAMBIOS**                                                                                          | â€”                    |

**Total: 1 funciÃ³n modificada + 1 regla CSS nueva (3 lÃ­neas). Sin nuevas dependencias.**

---

## âœ… CRITERIOS DE ACEPTACIÃ“N

1. Al hacer clic en "Ver N respuesta(s)" de un comentario con replies, las respuestas se muestran.
2. Al hacer clic en "Ocultar", las respuestas se ocultan.
3. El icono cambia de `fa-chevron-down` a `fa-chevron-up` y viceversa.
4. El label cambia de "Ver" a "Ocultar" y viceversa.
5. El layout flex del contenedor `.jd-replies` se preserva (las respuestas se apilan verticalmente con el `border-left` y el `padding-left`).
6. El listener de click se registra **una sola vez** en `document`, independientemente de cuÃ¡ntas veces se llame a `initToggleReplies()` (idempotencia).
7. El scroll infinito sigue funcionando: las respuestas de los comentarios cargados dinÃ¡micamente tambiÃ©n se pueden expandir/colapsar.
8. No se rompe ningÃºn otro comportamiento: el botÃ³n "Responder" sigue abriendo el formulario inline, el submit de comentarios sigue funcionando, las reacciones siguen funcionando, la eliminaciÃ³n como superuser sigue funcionando.
9. La pÃ¡gina se renderiza sin errores en consola (mÃ¡s allÃ¡ de los logs esperados del propio handler).

---

## ðŸ§ª PRUEBAS

### Pruebas manuales (navegador)

1. **Caso bÃ¡sico (sin scroll):**
   - Abrir un artÃ­culo con al menos un comentario que tenga respuestas.
   - Hacer clic en "Ver N respuesta(s)".
   - âœ… Las respuestas deben mostrarse con el borde lateral morado.
   - Hacer clic en "Ocultar".
   - âœ… Las respuestas deben ocultarse.

2. **Caso con scroll infinito:**
   - Abrir un artÃ­culo con >10 comentarios.
   - Hacer scroll hasta cargar al menos 2 lotes.
   - Probar el toggle en un comentario del **primer lote** y en uno del **Ãºltimo lote cargado**.
   - âœ… Ambos deben funcionar igual de bien.

3. **Caso superuser:**
   - Iniciar sesiÃ³n como superadmin.
   - En un comentario con respuestas, hacer clic en el toggle Y luego en el botÃ³n de eliminar (icono `fa-times`).
   - âœ… Ambos deben funcionar independientemente.

4. **Caso sin respuestas:**
   - Comentario sin replies â†’ no debe aparecer el botÃ³n "Ver respuestas" (esto ya funcionaba).

5. **Caso de cachÃ© del navegador:**
   - Hacer **hard refresh** (Ctrl+Shift+R) tras el fix.
   - âœ… El nuevo JS debe cargarse (verificar en DevTools â†’ Network â†’ `comments_handler.js?v=...`).

### Pruebas automÃ¡ticas (no aplican â€” JS del lado cliente)

No hay tests automatizados para JS del lado cliente en el proyecto. La verificaciÃ³n es 100% manual.

---

## ðŸ”— REFERENCIAS

- **DiagnÃ³stico completo:** `gestion_proyecto/diagnosticos/diagnostico_toggle_replies_blog_detail.md`
- **Template del blog detail:** `backend/blog/templates/blog/blog_detail.html` (lÃ­neas 516â€“564)
- **JS del toggle:** `backend/blog/static/blog/js/comments_handler.js` (funciÃ³n `initToggleReplies`, ~lÃ­neas 228â€“265)
- **CSS de respuestas:** `backend/blog/static/blog/css/blog_detail.css` (lÃ­neas 559â€“588, 2172â€“2180)

---

## âœ… CHECKLIST DE IMPLEMENTACIÃ“N (ESTADO REAL al 09/06/2026 - 11:12)

### âœ… COMPLETADO (Fases 1, 2, 2.5)

- [x] **Fase 1:** Reemplazar `initToggleReplies` en `comments_handler.js` (idempotente + classList)
  - Flag `document._jdToggleRepliesInit` aÃ±adido
  - Event delegation sobre `document` (un solo listener)
  - Uso de `classList.toggle('is-open')` en vez de `style.display` directo
- [x] **Fase 2:** AÃ±adir regla CSS `.jd-replies.is-open` en `blog_detail.css`
  - Regla aÃ±adida en lÃ­neas 2182-2187 del CSS
- [x] **Fase 2.5 (CRÃTICO - descubrimiento post-Fase 2):** AÃ±adir `style.display = 'flex'/'none'` en JS
  - **Motivo:** el `style="display:none"` inline del HTML tiene mayor especificidad que cualquier selector CSS
  - Sin esta Fase 2.5, la regla CSS `.jd-replies.is-open` NO habrÃ­a sobrescrito el inline
- [x] **Test automatizado del fix (PASSED):** simulaciÃ³n de 3 clicks consecutivos
  - Click 1: ABRE (display=flex, is-open=true)
  - Click 2: CIERRA (display=none, is-open=false)
  - Click 3: RE-ABRE (display=flex, is-open=true)
- [x] **ValidaciÃ³n de sintaxis JS:** `node --check backend/blog/static/blog/js/comments_handler.js` â†’ OK
- [x] **Limpieza de archivos temporales** (scripts .ps1, _test_fix.js eliminados)

### â³ PENDIENTE (Fase 3 - requiere navegador)

- [ ] **Fase 3:** Validar manualmente comportamiento tras scroll infinito
- [ ] Hard refresh (Ctrl+Shift+R) y verificar carga del nuevo JS
- [ ] Caso bÃ¡sico: toggle en comentario con respuestas
- [ ] Caso con scroll: toggle tras cargar varios lotes
- [ ] Caso superuser: toggle + delete independientes
- [ ] `python manage.py check` sin errores

### âœ… COMPLETADO (Fase 4 - opcional)

- [x] Comentar CSS huÃ©rfano de Alpine (lÃ­neas ~993-1010 de `blog_detail.css`)
  - Bloque `/* Transiciones Alpine */` ahora estÃ¡ comentado (no eliminado) con cabecera explicativa
  - Trazabilidad histÃ³rica preservada (regla de oro: no borrar codigo existente)