# HU-17.18 Filtros Especiales del Dashboard — Autor, Peso, Fieldset y Filtros Colapsables

## Objetivo
Mejorar la experiencia de usuario en el dashboard de moderación agregando:
1. **Filtro de autor con autocomplete AJAX** — input que sugiere autores conforme se escribe (vanilla JS `fetch()`, sin librerías externas)
2. **Filtro de orden por peso de archivos** — ordenar artículos por tamaño de recursos
3. **Fieldset/legend consistente** — envolver filtros en fieldset con legend "Filtros de búsqueda" (misma estructura que `blog_list.html`)
4. **🎯 Filtros avanzados colapsables por defecto** — para no saturar la UI, los filtros especiales (publicación, moderación, autor, orden) aparecen colapsados. Solo el buscador principal `#blog-search-input` está visible inicialmente.

---

## 🔍 Decisión de diseño — Filtros colapsables

### Problema detectado
La HU original proponía mostrar todos los filtros en 2 filas visibles siempre. Esto satura visualmente el dashboard para el caso de uso más frecuente: buscar un artículo por título/slug.

### Análisis de usabilidad
| Aspecto                    | Filtros siempre visibles             | Filtros colapsables ✅                      |
| -------------------------- | ------------------------------------ | ------------------------------------------ |
| **Carga cognitiva**        | Alta: 4-6 controles visibles siempre | Baja: solo input de búsqueda + toggle      |
| **Progressive disclosure** | ❌ Muestra todo de golpe              | ✅ Muestra lo básico, avanzado bajo demanda |
| **Casos de uso frecuente** | Buscar por título (80%)              | Buscar por título (80%)                    |
| **Casos de uso avanzado**  | Filtrar por autor/peso (20%)         | Filtrar por autor/peso (20%)               |
| **Patrón moderno**         | ❌ Obsoleto                           | ✅ Airbnb, GitHub, Google lo usan           |

### Diseño visual final

```
┌──────────────────────────────────────────────────────────────────────┐
│ Fila 1 — Siempre visible (sticky)                                    │
│ [🔍 Buscar por título, slug...                    ] [Buscar] [⚙️]    │
│ "12 artículos encontrados" (contador, solo si hay búsqueda)          │
│                                                                      │
│ Fila 2 — Colapsable (toggle con ⚙️)                                  │
│ ┌──────────────────────────────────────────────────────────────────┐ │
│ │ [Publicación ▾] [Moderación ▾] [🔍 Autor...] [Ordenar por ▾]     │ │
│ │                   [🧹 Filtrar] [✕ Limpiar]                       │ │
│ └──────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│ Fila 3 — Siempre visible si hay filtros activos                      │
│ [🔍 "titulo" ✕] [✅ Publicados ✕] [👤 jaime ✕] [📦 Mayor peso ✕]   │
│ [🧹 Limpiar todo]                                                    │
└──────────────────────────────────────────────────────────────────────┘
```

### Principios aplicados
1. **Progressive disclosure**: primero lo simple, lo complejo bajo demanda
2. **Active filter chips siempre visibles**: el usuario nunca olvida que tiene filtros aplicados
3. **Sticky solo la fila 1 + chips**: el toggle de filtros avanzados y los chips siempre accesibles al hacer scroll
4. **Toggle con icono ⚙️ y texto**: indica claramente que hay más opciones disponibles
5. **aria-expanded**: soporte de accesibilidad para el toggle collapse

---

## 📦 Artefactos funcionales — Especificación backend ↔ frontend

### 🧠 Contrato de comunicación

Cada filtro se comunica vía parámetros GET en la URL. La vista `dashboard_view()` los recibe y los aplica secuencialmente.

| Parámetro GET  | Valores posibles                                 | Default  | Backend (views.py)                                                              | Frontend (HTML)                             |
| -------------- | ------------------------------------------------ | -------- | ------------------------------------------------------------------------------- | ------------------------------------------- |
| `q`            | string                                           | `""`     | Filtra por `title__icontains`, `slug__icontains`, `author__username__icontains` | Input `#blog-search-input`                  |
| `is_published` | `""` / `"1"` / `"0"`                             | `""`     | `is_published=True` si `"1"`, `is_published=False` si `"0"`                     | Select `[name="is_published"]`              |
| `moderation`   | `""` / `"pending"` / `"approved"` / `"rejected"` | `""`     | `moderation_status=valor` si está en la lista                                   | Select `[name="moderation"]`                |
| `author`       | string (username)                                | `""`     | Filtra por `author__username__icontains` (solo username, no full_name)          | Input `#author-autocomplete-input` con AJAX |
| `sort`         | `"date"` / `"weight_desc"` / `"weight_asc"`      | `"date"` | Ver sección "Orden por peso" abajo                                              | Select `[name="sort"]`                      |

### ⚠️ Diferencia entre `q` y `author`

| Aspecto         | `q` (búsqueda general)                | `author` (filtro específico)                 |
| --------------- | ------------------------------------- | -------------------------------------------- |
| **Campos**      | title, slug, author__username         | author__username (solo coincidencia exacta)  |
| **Interfaz**    | Input de texto libre                  | Autocomplete con sugerencias AJAX            |
| **Debounce**    | 500ms (búsqueda en vivo)              | 300ms (sugerencias)                          |
| **Caso de uso** | "Busca el artículo que habla sobre X" | "Enséñame todos los artículos de este autor" |

---

## 🔧 Backend — Especificación técnica detallada

### 1. Endpoint: `api_authors_autocomplete(request)` — NUEVO en `views.py`

```python
@require_http_methods(["GET"])
def api_authors_autocomplete(request):
    """Endpoint AJAX para autocompletar autores en el dashboard.
    
    Busca en User (first_name, last_name, username) con icontains.
    - Superadmin/staff: ve todos los usuarios (máx 8)
    - Usuario normal: solo ve su propio nombre
    
    Retorna:
        JsonResponse({"authors": [
            {"id": 1, "username": "jaime", "display": "Jaime Díaz"},
            ...
        ]})
    """
    q = request.GET.get("q", "").strip()
    if len(q) < 2:
        return JsonResponse({"authors": []})
    
    if request.user.is_superuser or request.user.is_staff:
        users = User.objects.filter(
            Q(first_name__icontains=q) |
            Q(last_name__icontains=q) |
            Q(username__icontains=q)
        ).distinct()[:8]
    else:
        # Usuario normal solo ve su propio nombre
        users = User.objects.filter(id=request.user.id)
    
    authors = []
    for u in users:
        display = u.get_full_name() or u.username
        authors.append({
            "id": u.id,
            "username": u.username,
            "display": display,
        })
    
    return JsonResponse({"authors": authors})
```

**Ruta en `urls.py`** (agregar ANTES de `<slug>`):
```python
path("api/authors/autocomplete/", views.api_authors_autocomplete, name="api_authors_autocomplete"),
```

### 2. Vista `dashboard_view()` — MODIFICACIONES

**Ubicación**: `views.py` líneas 181-302

**Cambios necesarios**:

#### a) Nuevos imports (si no existen)
```python
from django.db.models import Case, When, Value, IntegerField
```
Nota: `Q` y `Count` ya están importados (línea 27).

#### b) Filtrar por autor (después del filtro `search_query`)
```python
# Filtro específico de autor (NUEVO)
author_filter = request.GET.get("author", "").strip()
if author_filter:
    posts = posts.filter(author__username__icontains=author_filter)
```

**Importante**: El filtro `author` usa `icontains` sobre `username` para que funcione con autocomplete parcial. El autocomplete devuelve usernames exactos, pero al recargar si el usuario escribió parcialmente, sigue funcionando.

#### c) Orden por peso (REEMPLAZA el order_by fijo actual)
```python
# Orden (NUEVO) — reemplaza `posts = posts.order_by("-publish_date")`
sort = request.GET.get("sort", "date")

if sort in ("weight_desc", "weight_asc"):
    # ⚠️ O(N): calcular peso de TODOS los posts filtrados
    # Esto es necesario porque necesitamos el peso para ordenar.
    # Para blogs con hasta 500 posts es aceptable.
    post_list = list(posts)  # evaluar queryset
    for post in post_list:
        info = get_post_files_info(post.slug)
        post._total_size_bytes = info["total_size_bytes"]
    
    reverse_order = (sort == "weight_desc")
    post_list.sort(key=lambda p: p._total_size_bytes, reverse=reverse_order)
    post_ids = [p.id for p in post_list]
    
    # Preservar el orden usando Case/When
    preserved = Case(*[When(id=pid, then=pos) for pos, pid in enumerate(post_ids)])
    posts = BlogPost.objects.filter(id__in=post_ids).annotate(
        sort_order=preserved
    ).order_by("sort_order")
else:
    posts = posts.order_by("-publish_date")
```

**⚠️ Advertencia de rendimiento**: `get_post_files_info()` escanea disco (blogs_source/<slug>/). Para 500 posts con archivos pesados, puede tomar 1-2 segundos. Es aceptable porque:
- Solo se activa cuando el usuario selecciona explícitamente "Mayor peso" o "Menor peso"
- No es el caso de uso default
- Si en futuro hay problemas, se puede cachear el peso en el modelo BlogPost

#### d) Pasar nuevas variables al context (dentro del render, después de `search_query`)
```python
# Pasar context actualizado
context = {
    "page_obj": page_obj,
    "stats": stats,
    "is_published_filter": is_published_filter,
    "moderation_filter": moderation_filter,
    "search_query": search_query,
    "author_filter": author_filter,           # NUEVO
    "sort_filter": sort,                       # NUEVO
    "query_string": query_string,
    "comment_counts": comment_counts,
    "email_config": email_config,
    # NUEVO: True si hay algún filtro avanzado activo (para autoexpandir collapse)
    "has_active_filters": bool(
        author_filter
        or sort != "date"
        or is_published_filter
        or moderation_filter
    ),
}
```

**⚠️ NOTA IMPORTANTE**: El context actual de `dashboard_view()` usa `return render(request, "blog/dashboard.html", {...})` con argumentos directos (líneas 289-301). El refactor a diccionario `context` solo es organizativo, no cambia funcionalidad.

#### e) Reubicación de `files_info` para compatibilidad con sort por peso

Actualmente `files_info` se calcula DESPUÉS de paginar (líneas 252-254). Si `sort=weight_desc/asc`, el peso ya se calculó antes. Para no calcularlo dos veces:

```python
# HU-011.10: Información de archivos asociados (solo superadmin)
if is_super:
    for post in page_obj:
        # Si ya se calculó por sort por peso, reusar
        if hasattr(post, '_total_size_bytes'):
            # Reconstruir files_info a partir del peso ya calculado
            # (esto evita escanear disco dos veces)
            info = get_post_files_info(post.slug)  # ya está cacheado por OS
        else:
            info = get_post_files_info(post.slug)
        post.files_info = info
```

O más simple, siempre calcular `files_info` después de paginar como ahora, independientemente del sort. El costo es aceptable porque solo son 20 posts por página.

---

## 🎨 Frontend — Especificación técnica detallada

### 3. Template `dashboard.html` — MODIFICACIONES

**Reemplazar el bloque de filtros actual (líneas 67-98)** por este diseño de 3 filas:

```html
<!-- ============================================= -->
<!-- HU-17.18: Filtros colapsables por defecto     -->
<!-- NOTA: Toda la sección de filtros reemplaza el  -->
<!-- bloque actual: <div class="card border-0..."> -->
<!-- ============================================= -->
<div class="card border-0 shadow-sm rounded-3 mb-4">
    <div class="card-body p-3">
        <form method="GET" action="{% url 'blog:dashboard' %}" class="blog-search-form" role="search">
            <fieldset class="search-filters">
                <legend class="main-title-filters">Filtros de búsqueda</legend>

                <!-- ════════ FILA 1: BUSCADOR PRINCIPAL + TOGGLE ════════ -->
                <!-- Siempre visible + sticky -->
                <div class="blog-search-second-row" id="filters-sticky-row">
                    <div class="d-flex gap-2 align-items-center flex-wrap">
                        <div class="input-group blog-search-group flex-grow-1" style="min-width:200px;">
                            <span class="input-group-text blog-search-icon">
                                <i class="fas fa-search"></i>
                            </span>
                            <input type="text"
                                name="q"
                                id="blog-search-input"
                                class="form-control blog-search-input"
                                placeholder="Buscar por título, slug o autor..."
                                value="{{ search_query }}"
                                aria-label="Buscar artículos">
                            {# Botón X para limpiar — se inyecta con JS #}
                            <button type="submit" class="btn blog-search-btn">
                                <span>Buscar</span>
                            </button>
                        </div>

                        <!-- Botón toggle para filtros avanzados -->
                        <button type="button"
                            id="toggle-advanced-filters"
                            class="btn btn-sm btn-outline-secondary rounded-pill px-3"
                            style="font-size:.82rem; white-space:nowrap;"
                            data-bs-toggle="collapse"
                            data-bs-target="#advanced-filters-collapse"
                            aria-expanded="false"
                            aria-controls="advanced-filters-collapse"
                            title="Mostrar/ocultar filtros avanzados">
                            <i class="fas fa-cog me-1"></i>
                            <span id="toggle-advanced-text">Filtros avanzados</span>
                        </button>
                    </div>

                    <!-- Contador de resultados -->
                    {% if search_query or has_active_filters %}
                    <div class="mt-1" style="font-size:.78rem; color:#6b7280;">
                        <i class="fas fa-info-circle me-1"></i>
                        {{ page_obj.paginator.count }} artículo{{ page_obj.paginator.count|pluralize:'s' }} encontrado{{ page_obj.paginator.count|pluralize:'s' }}
                    </div>
                    {% endif %}
                </div>

                <!-- ════════ FILA 2: FILTROS AVANZADOS (COLAPSABLE) ════════ -->
                <div class="collapse{% if has_active_filters %} show{% endif %}" id="advanced-filters-collapse">
                    <div class="blog-toolbar-row mt-2 pt-2" style="border-top:1px solid #f3f4f6;">
                        <div class="d-flex gap-2 flex-wrap align-items-end">
                            <div class="col-6 col-md-2">
                                <label class="form-label mb-1 fw-semibold" style="font-size:.78rem;">Publicación</label>
                                <select name="is_published" class="form-select">
                                    <option value="">Todos</option>
                                    <option value="1" {% if is_published_filter == "1" %}selected{% endif %}>✅ Publicados</option>
                                    <option value="0" {% if is_published_filter == "0" %}selected{% endif %}>📝 Borradores</option>
                                </select>
                            </div>
                            <div class="col-6 col-md-2">
                                <label class="form-label mb-1 fw-semibold" style="font-size:.78rem;">Moderación</label>
                                <select name="moderation" class="form-select">
                                    <option value="">Todas</option>
                                    <option value="pending" {% if moderation_filter == "pending" %}selected{% endif %}>⏳ Pendiente</option>
                                    <option value="approved" {% if moderation_filter == "approved" %}selected{% endif %}>✅ Aprobado</option>
                                    <option value="rejected" {% if moderation_filter == "rejected" %}selected{% endif %}>❌ Rechazado</option>
                                </select>
                            </div>

                            <!-- Filtro de autor con autocomplete (AJAX) -->
                            <div class="author-autocomplete-wrapper position-relative" style="min-width:140px;">
                                <label class="form-label mb-1 fw-semibold" style="font-size:.78rem;">Autor</label>
                                <input type="text"
                                       name="author"
                                       id="author-autocomplete-input"
                                       class="form-control"
                                       placeholder="Escribe un nombre..."
                                       value="{{ author_filter }}"
                                       autocomplete="off">
                                <div id="author-suggestions" class="author-suggestions-dropdown d-none"></div>
                            </div>

                            <!-- Filtro de ordenar por -->
                            <div class="col-6 col-md-2">
                                <label class="form-label mb-1 fw-semibold" style="font-size:.78rem;">Ordenar por</label>
                                <select name="sort" class="form-select">
                                    <option value="date" {% if sort_filter == "date" %}selected{% endif %}>📅 Más recientes</option>
                                    <option value="weight_desc" {% if sort_filter == "weight_desc" %}selected{% endif %}>📦 Mayor peso</option>
                                    <option value="weight_asc" {% if sort_filter == "weight_asc" %}selected{% endif %}>📦 Menor peso</option>
                                </select>
                            </div>

                            <div class="blog-toolbar-actions d-flex gap-1 pb-1">
                                <button type="submit" class="btn blog-date-apply-btn" title="Aplicar filtros">
                                    <i class="fas fa-search"></i> Filtrar
                                </button>
                                <a href="{% url 'blog:dashboard' %}" class="btn blog-date-clear-btn" title="Limpiar todos los filtros">
                                    <i class="fas fa-times-circle"></i> Limpiar
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </fieldset>
        </form>

        <!-- ════════ FILA 3: ACTIVE FILTER CHIPS ════════ -->
        <!-- Siempre visibles si hay filtros activos -->
        {% if search_query or is_published_filter or moderation_filter or author_filter or sort_filter != "date" %}
        <div class="active-filter-chips" id="filter-chips-row">
            <!-- Cada chip elimina SOLO ese filtro preservando los demás -->
            {% if search_query %}
            <span class="chip badge rounded-pill">
                🔍 "{{ search_query }}"
                <a href="?{% if is_published_filter %}&is_published={{ is_published_filter }}{% endif %}{% if moderation_filter %}&moderation={{ moderation_filter }}{% endif %}{% if author_filter %}&author={{ author_filter }}{% endif %}{% if sort_filter != 'date' %}&sort={{ sort_filter }}{% endif %}" class="chip-remove" title="Eliminar búsqueda">✕</a>
            </span>
            {% endif %}

            {% if is_published_filter == "1" %}
            <span class="chip badge rounded-pill">
                ✅ Publicados
                <a href="?{% if search_query %}&q={{ search_query }}{% endif %}{% if moderation_filter %}&moderation={{ moderation_filter }}{% endif %}{% if author_filter %}&author={{ author_filter }}{% endif %}{% if sort_filter != 'date' %}&sort={{ sort_filter }}{% endif %}" class="chip-remove" title="Eliminar filtro de publicación">✕</a>
            </span>
            {% elif is_published_filter == "0" %}
            <span class="chip badge rounded-pill">
                📝 Borradores
                <a href="?{% if search_query %}&q={{ search_query }}{% endif %}{% if moderation_filter %}&moderation={{ moderation_filter }}{% endif %}{% if author_filter %}&author={{ author_filter }}{% endif %}{% if sort_filter != 'date' %}&sort={{ sort_filter }}{% endif %}" class="chip-remove" title="Eliminar filtro de publicación">✕</a>
            </span>
            {% endif %}

            {% if moderation_filter %}
            <span class="chip badge rounded-pill">
                {% if moderation_filter == "pending" %}⏳ Pendiente
                {% elif moderation_filter == "approved" %}✅ Aprobado
                {% elif moderation_filter == "rejected" %}❌ Rechazado{% endif %}
                <a href="?{% if search_query %}&q={{ search_query }}{% endif %}{% if is_published_filter %}&is_published={{ is_published_filter }}{% endif %}{% if author_filter %}&author={{ author_filter }}{% endif %}{% if sort_filter != 'date' %}&sort={{ sort_filter }}{% endif %}" class="chip-remove" title="Eliminar filtro de moderación">✕</a>
            </span>
            {% endif %}

            {% if author_filter %}
            <span class="chip badge rounded-pill">
                👤 {{ author_filter }}
                <a href="?{% if search_query %}&q={{ search_query }}{% endif %}{% if is_published_filter %}&is_published={{ is_published_filter }}{% endif %}{% if moderation_filter %}&moderation={{ moderation_filter }}{% endif %}{% if sort_filter != 'date' %}&sort={{ sort_filter }}{% endif %}" class="chip-remove" title="Eliminar filtro de autor">✕</a>
            </span>
            {% endif %}

            {% if sort_filter == "weight_desc" %}
            <span class="chip badge rounded-pill">
                📦 Mayor peso
                <a href="?{% if search_query %}&q={{ search_query }}{% endif %}{% if is_published_filter %}&is_published={{ is_published_filter }}{% endif %}{% if moderation_filter %}&moderation={{ moderation_filter }}{% endif %}{% if author_filter %}&author={{ author_filter }}{% endif %}" class="chip-remove" title="Eliminar orden">✕</a>
            </span>
            {% elif sort_filter == "weight_asc" %}
            <span class="chip badge rounded-pill">
                📦 Menor peso
                <a href="?{% if search_query %}&q={{ search_query }}{% endif %}{% if is_published_filter %}&is_published={{ is_published_filter }}{% endif %}{% if moderation_filter %}&moderation={{ moderation_filter }}{% endif %}{% if author_filter %}&author={{ author_filter }}{% endif %}" class="chip-remove" title="Eliminar orden">✕</a>
            </span>
            {% endif %}

            <!-- "Limpiar todo" siempre visible si hay al menos 1 filtro -->
            <a href="{% url 'blog:dashboard' %}" class="chip chip-clear-all badge rounded-pill">
                🧹 Limpiar todo
            </a>
        </div>
        {% endif %}
    </div>
</div>
```

### 4. CSS `dashboard.css` — NUEVOS ESTILOS

```css
/* =============================================
   HU-17.18: Autocomplete de autores
   ============================================= */

.author-autocomplete-wrapper {
    position: relative;
}

.author-suggestions-dropdown {
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    background: #fff;
    border: 1px solid #e2e8f0;
    border-radius: 10px;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.12);
    z-index: 1000;
    max-height: 200px;
    overflow-y: auto;
    margin-top: 4px;
}

.author-suggestion-item {
    padding: 8px 12px;
    cursor: pointer;
    font-size: 0.85rem;
    color: #374151;
    border-bottom: 1px solid #f3f4f6;
    transition: background 0.15s ease;
}

.author-suggestion-item:last-child {
    border-bottom: none;
}

.author-suggestion-item:hover {
    background: #f3f0ff;
    color: #6f42c1;
}

.author-suggestions-dropdown::-webkit-scrollbar {
    width: 6px;
}

.author-suggestions-dropdown::-webkit-scrollbar-thumb {
    background: #d1d5db;
    border-radius: 3px;
}

.author-suggestions-dropdown::-webkit-scrollbar-thumb:hover {
    background: #9ca3af;
}

/* =============================================
   HU-17.18: Active Filter Chips (estilo Airbnb)
   ============================================= */

.active-filter-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    padding: 8px 4px 0 4px;
    align-items: center;
}

.chip {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    background: #f3f0ff;
    color: #6f42c1;
    border: 1px solid #d8cff5;
    font-size: 0.78rem;
    font-weight: 500;
    padding: 4px 10px;
    border-radius: 20px;
    transition: all 0.15s ease;
    text-decoration: none;
}

.chip:hover {
    background: #ede7ff;
    border-color: #c4b5fd;
}

.chip-remove {
    color: #6f42c1;
    text-decoration: none;
    font-size: 0.7rem;
    opacity: 0.6;
    transition: opacity 0.15s ease;
    margin-left: 2px;
    line-height: 1;
}

.chip-remove:hover {
    opacity: 1;
    color: #dc2626;
}

.chip-clear-all {
    background: #fef2f2;
    color: #dc2626;
    border-color: #fecaca;
    font-weight: 600;
    text-decoration: none;
}

.chip-clear-all:hover {
    background: #fee2e2;
    color: #b91c1c;
    border-color: #fca5a5;
}

/* =============================================
   HU-17.18: Sticky para buscador y chips
   ============================================= */

#filters-sticky-row {
    position: sticky;
    top: 0;
    z-index: 100;
    background: #fff;
    padding: 4px 0;
    transition: box-shadow 0.2s ease;
}

#filters-sticky-row.is-sticky {
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
}

#filter-chips-row {
    position: sticky;
    top: 52px; /* Altura del buscador + padding */
    z-index: 99;
    background: #fff;
    padding: 4px 0;
    transition: box-shadow 0.2s ease;
}

#filter-chips-row.is-sticky {
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
}

/* El collapse de filtros avanzados NO es sticky */
#advanced-filters-collapse {
    /* No sticky */
}

/* =============================================
   HU-17.18: Botón X del buscador (inyectado con JS)
   ============================================= */

.blog-search-group {
    position: relative;
}

.blog-search-group .btn-clear-search {
    position: absolute;
    right: 80px;
    top: 50%;
    transform: translateY(-50%);
    z-index: 10;
    padding: 2px 6px;
    font-size: .75rem;
    color: #9ca3af;
    background: transparent;
    border: none;
    display: none;
    cursor: pointer;
    transition: color 0.15s ease;
}

.blog-search-group .btn-clear-search:hover {
    color: #6b7280;
}
```

### 5. JavaScript `dashboard.js` — 3 NUEVOS BLOQUES (al final del archivo)

**Bloque 1: Autocomplete AJAX de autores**

```javascript
// =============================================
// HU-17.18: Autocomplete AJAX de autores
// =============================================
(function() {
    var input = document.getElementById('author-autocomplete-input');
    var suggestionsDiv = document.getElementById('author-suggestions');
    if (!input || !suggestionsDiv) return;

    var debounceTimer;

    input.addEventListener('input', function() {
        clearTimeout(debounceTimer);
        var q = this.value.trim();

        if (q.length < 2) {
            suggestionsDiv.classList.add('d-none');
            return;
        }

        debounceTimer = setTimeout(function() {
            fetch('/blog/api/authors/autocomplete/?q=' + encodeURIComponent(q))
                .then(function(response) { return response.json(); })
                .then(function(data) {
                    if (data.authors.length === 0) {
                        suggestionsDiv.classList.add('d-none');
                        return;
                    }

                    suggestionsDiv.innerHTML = data.authors.map(function(a) {
                        return '<div class="author-suggestion-item" data-username="' + a.username + '" data-display="' + a.display + '">' + a.display + '</div>';
                    }).join('');
                    suggestionsDiv.classList.remove('d-none');

                    Array.from(suggestionsDiv.querySelectorAll('.author-suggestion-item')).forEach(function(item) {
                        item.addEventListener('click', function() {
                            input.value = this.dataset.username;
                            suggestionsDiv.classList.add('d-none');
                            input.closest('form').submit();
                        });
                    });
                })
                .catch(function() {
                    suggestionsDiv.classList.add('d-none');
                });
        }, 300);
    });

    document.addEventListener('click', function(e) {
        if (!input.contains(e.target) && !suggestionsDiv.contains(e.target)) {
            suggestionsDiv.classList.add('d-none');
        }
    });
})();
```

**Bloque 2: Toggle de filtros avanzados (texto dinámico)**

```javascript
// =============================================
// HU-17.18: Toggle collapse filtros avanzados
// =============================================
(function() {
    var toggleBtn = document.getElementById('toggle-advanced-filters');
    var collapseEl = document.getElementById('advanced-filters-collapse');
    if (!toggleBtn || !collapseEl) return;

    function updateToggleText(isExpanded) {
        if (isExpanded) {
            toggleBtn.innerHTML = '<i class="fas fa-times me-1"></i><span id="toggle-advanced-text">Cerrar filtros</span>';
            toggleBtn.setAttribute('aria-expanded', 'true');
        } else {
            toggleBtn.innerHTML = '<i class="fas fa-cog me-1"></i><span id="toggle-advanced-text">Filtros avanzados</span>';
            toggleBtn.setAttribute('aria-expanded', 'false');
        }
    }

    // Escuchar eventos de Bootstrap Collapse
    collapseEl.addEventListener('shown.bs.collapse', function() { updateToggleText(true); });
    collapseEl.addEventListener('hidden.bs.collapse', function() { updateToggleText(false); });

    // Estado inicial
    if (collapseEl.classList.contains('show')) {
        updateToggleText(true);
    }
})();
```

**Bloque 3: Mejoras al buscador (botón X + debounce + Escape)**

```javascript
// =============================================
// HU-17.18: Mejoras al buscador #blog-search-input
// =============================================
(function() {
    var searchInput = document.getElementById('blog-search-input');
    if (!searchInput) return;

    var form = searchInput.closest('form');
    var debounceTimer;

    // 1. Botón X para limpiar — crear dinámicamente
    var clearBtn = document.createElement('button');
    clearBtn.type = 'button';
    clearBtn.className = 'btn-clear-search';
    clearBtn.innerHTML = '<i class="fas fa-times-circle"></i>';
    clearBtn.title = 'Limpiar búsqueda';
    clearBtn.setAttribute('aria-label', 'Limpiar búsqueda');

    var inputGroup = searchInput.closest('.input-group');
    if (inputGroup) {
        inputGroup.appendChild(clearBtn);
    }

    function toggleClearBtn() {
        clearBtn.style.display = searchInput.value.trim() ? 'block' : 'none';
    }
    toggleClearBtn();
    searchInput.addEventListener('input', toggleClearBtn);

    clearBtn.addEventListener('click', function() {
        searchInput.value = '';
        toggleClearBtn();
        searchInput.focus();
        // Recargar sin el parámetro q (preservar otros filtros)
        var url = new URL(window.location.href);
        url.searchParams.delete('q');
        url.searchParams.delete('page');
        window.location.href = url.toString();
    });

    // 2. Búsqueda en vivo con debounce (500ms)
    searchInput.addEventListener('input', function() {
        clearTimeout(debounceTimer);
        var val = this.value.trim();
        debounceTimer = setTimeout(function() {
            if (val.length >= 2 || val.length === 0) {
                form.submit();
            }
        }, 500);
    });

    // 3. Atajo Escape → limpiar + cerrar foco
    searchInput.addEventListener('keydown', function(e) {
        if (e.key === 'Escape') {
            searchInput.value = '';
            toggleClearBtn();
            searchInput.blur();
        }
    });
})();
```

---

## 📋 Resumen de artefactos a modificar/crear

| Archivo                                      | Acción    | Cambios                                                                                 |
| -------------------------------------------- | --------- | --------------------------------------------------------------------------------------- |
| `backend/blog/views.py`                      | MODIFICAR | 1. Agregar `api_authors_autocomplete()` (nueva función)                                 |
|                                              |           | 2. En `dashboard_view()`: agregar filtro `author`, orden por peso, `has_active_filters` |
| `backend/blog/urls.py`                       | MODIFICAR | Agregar ruta `api/authors/autocomplete/`                                                |
| `backend/blog/templates/blog/dashboard.html` | MODIFICAR | Reemplazar bloque de filtros (3 filas: sticky + collapse + chips)                       |
| `backend/blog/static/blog/js/dashboard.js`   | MODIFICAR | Agregar 3 bloques al final del archivo (autocomplete, toggle, buscador)                 |
| `backend/blog/static/blog/css/dashboard.css` | MODIFICAR | Agregar estilos: autocomplete, chips, sticky, botón X                                   |

---

## ✅ Criterios de aceptación

### 🎯 Filtros Colapsables por Defecto
- [x] Los filtros avanzados aparecen colapsados por defecto (sin `has_active_filters`)
- [x] Solo el buscador `#blog-search-input` + botón "Buscar" + toggle `⚙️` están visibles inicialmente
- [x] Al hacer click en `⚙️ Filtros avanzados` se expanden/colapsan con animación Bootstrap
- [x] El toggle cambia de texto: `⚙️ Filtros avanzados` ↔ `✕ Cerrar filtros`
- [x] El toggle usa `aria-expanded` y `aria-controls`
- [x] Si hay filtros activos, el collapse se expande automáticamente al cargar

### 🔍 Buscador #blog-search-input
- [x] Botón X dentro del input (se inyecta con JS) que aparece cuando hay texto
- [x] Click en X → elimina solo `q` de la URL, preserva demás filtros, recarga
- [x] Búsqueda en vivo con debounce 500ms
- [x] Atajo Escape: limpia campo y cierra foco
- [x] Contador de resultados: "N artículos encontrados" (visible si hay `search_query` o `has_active_filters`)

### 👤 Autocomplete de Autor (AJAX)
- [x] Input `#author-autocomplete-input` dentro de los filtros avanzados
- [x] Al escribir 2+ caracteres, debounce 300ms → fetch a `/blog/api/authors/autocomplete/`
- [x] Dropdown con máximo 8 sugerencias
- [x] Click en sugerencia → llena input con username → submit del form
- [x] Click fuera → cierra dropdown
- [x] Parámetro `author` se conserva al paginar
- [x] Superadmin ve todos los autores; usuario normal solo su propio nombre

### 📦 Orden por Peso
- [x] Select con opciones: "📅 Más recientes" (default), "📦 Mayor peso", "📦 Menor peso"
- [x] `get_post_files_info()` se calcula para TODOS los posts filtrados antes de paginar
- [x] Orden se preserva al cambiar de página (requiere recalcular en cada request)
- [x] Rendimiento aceptable para hasta 500 posts

### 🏷️ Active Filter Chips
- [x] Fila de chips visible solo si hay filtros activos
- [x] Cada chip tiene ✕ que elimina SOLO ese filtro (preserva los demás)
- [x] Chip "🧹 Limpiar todo" siempre visible si hay al menos 1 filtro activo
- [x] Chips con estilo `badge rounded-pill` morado

### 📌 Sticky
- [x] `#filters-sticky-row` (buscador + toggle) se queda fijo al hacer scroll
- [x] `#filter-chips-row` se queda fijo debajo del buscador
- [x] Sombra sutil cuando están en sticky (`.is-sticky`)
- [x] El collapse de filtros avanzados NO es sticky

### 📭 Empty State Mejorado
- [x] Empty state actual (`<div class="text-center py-5">`) se mantiene si no hay filtros activos
- [x] Si hay filtros activos Y no hay resultados, se muestra mensaje con sugerencias y botón "Limpiar filtros"

---

## 📐 Pasos de implementación (fases)

### Fase 1 — Backend: Endpoint autocomplete + filtro autor
1. Agregar `api_authors_autocomplete()` en `views.py` (después de `delete_blog_view`, antes del editor)
2. Agregar ruta en `urls.py`: `path("api/authors/autocomplete/", views.api_authors_autocomplete, name="api_authors_autocomplete")`
3. En `dashboard_view()`: agregar filtro `author` y `has_active_filters` al context
4. Verificar: `python manage.py check` sin errores

### Fase 2 — Backend: Filtro orden por peso
1. En `dashboard_view()`: reemplazar `posts = posts.order_by("-publish_date")` por lógica de sort
2. Implementar `Case/When` para preservar orden con peso
3. Pasar `sort_filter` al context
4. Verificar: `python manage.py check` sin errores

### Fase 3 — Template: Filtros colapsables + chips
1. Reemplazar bloque de filtros en `dashboard.html` (3 filas)
2. Agregar Active Filter Chips con enlaces de eliminación individual
3. Verificar: template carga sin errores

### Fase 4 — JavaScript: 3 bloques nuevos
1. Autocomplete de autores (debounce 300ms, fetch, dropdown, click→submit)
2. Toggle collapse (texto dinámico basado en eventos `shown/hidden.bs.collapse`)
3. Mejoras buscador (botón X, debounce 500ms, Escape)

### Fase 5 — CSS: Estilos
1. Autocomplete dropdown (position absolute, scrollbar)
2. Active Filter Chips (`.chip`, `.chip-remove`, `.chip-clear-all`)
3. Sticky (`.is-sticky` con box-shadow)
4. Botón X (`.btn-clear-search`)
5. Verificar responsive

### Fase 6 — Testing
1. Colapsable: defecto cerrado, toggle, autoexpand con filtros activos
2. Autocomplete: 300ms debounce, fetch, dropdown, click, cierre
3. Buscador: botón X, debounce 500ms, Escape
4. Chips: cada ✕ elimina solo ese filtro, "Limpiar todo" resetea
5. Orden: recientes, mayor/menor peso
6. Combinaciones: autor + publicación + moderación + búsqueda
7. Sticky: scroll, sombra
8. Paginación: todos los parámetros se conservan

---

### Checklist de la HU
- [x] Fase 1: Backend — endpoint autocomplete + filtro autor + has_active_filters
- [x] Fase 2: Backend — filtro orden por peso + sort_filter
- [x] Fase 3: Template — filtros colapsables + chips + contador
- [x] Fase 4: JavaScript — autocomplete + toggle + buscador
- [x] Fase 5: CSS — autocomplete + chips + sticky + botón X
- [x] Fase 6: Testing y validación (`python manage.py check` ✅ sin errores)
